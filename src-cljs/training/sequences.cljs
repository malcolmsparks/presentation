(ns training.sequences)

(def model
  (atom
   {:current-slide 1
    :slides
    [{:title "Sequences"}

     {:subtitle "Sequences"
      :bullets ["Collections, strings, maps and other data structures can be treated as sequences"]}

     ]}))
